if foo =~ '[a-z]' then
    println 'contains small letter'
fi

matcher = like 'pattern.*!' str;
(
    match = matcher ();
    print match;
    match != [];
) loop;

join '' (matchAll '\d+' do s: str (number s.[0] + 1) done id str)
join '' (matchAll '\d+' (str . (+1) . number . head) id str)
join '' (matchAll ',\s+' (const ';') id str)
replaceAll ',\s+' ';' str
split ',\s+' str

